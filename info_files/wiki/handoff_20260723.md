# Handoff 2026-07-23

## Goal of next session
Train a usable checkpoint for action space ~215, smoke-test play-vs-AI, optionally apply ponytail cuts.

## State of play
- Engine correctness fixes landed: capture clears mid-goat; adjacency is symmetric generated graph; goat-to-move with no moves → `draw`.
- MCTS uses full `snapshot()` (history preserved); NN still uses `serialize_state_binary()` only.
- API AI fallback + `/ai-move` already present; fallback contract test needs `fastapi` installed to run.
- Stack otherwise ready: self-play, train loop, FastAPI UI, Docker/CI.
- See `info_files/wiki/context.md` and `info_files/wiki/current_status.md`.

## Open decisions
- Ponytail cuts before first long train, or train first?
- Smoke train vs full `config.yaml` (20×20 games / 100 sims).
- Shrink net (8×512) for laptop / free-tier?

## Skills to use
- `/ponytail` (already active this session) for cuts or minimal train wiring.
- `/verification-before-completion` before claiming train/API success.
- `/handoff` at session end.

## Artifacts
- `baghchal.py` — capture fix, adjacency generator, goat-draw terminal, snapshot/clone
- `tests/test_engine_rules.py` — capture / adjacency / goat-draw cases
- `tests/test_api_contract.py` — includes MCTS-failure fallback test (needs fastapi)
- `game_actions.py` — catalog derived from adjacency (~215)
- `api/app.py`, `training/mcts.py`, `main.py`, `config.yaml`
- `info_files/wiki/context.md`, `current_status.md`, `state_map.md`
